name: Build Step

on:
  workflow_dispatch:  # <- SOLO manual mientras diagnosticamos

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build deshabilitado temporalmente (solo manual)."
